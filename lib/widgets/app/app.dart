import 'package:flutter/material.dart';

class FlutballManager extends StatelessWidget {
  const FlutballManager({super.key});

  @override
  Widget build(BuildContext context) {
    return const MaterialApp(
      title: 'Flutter Demo',
      home: SizedBox.shrink(),
    );
  }
}
